reservcar.app.br, www.reservcar.app.br {
  encode gzip

  # API - remove /api do path quando envia pro backend
  handle_path /api/* {
    reverse_proxy api:3000
  }

  # Frontend (Vite preview na porta 5173)
  reverse_proxy web:5173
}
