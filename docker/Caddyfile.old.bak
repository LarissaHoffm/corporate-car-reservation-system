/**{
  auto_https off
}

:80 {
  encode gzip

  # API (remove /api do path ao repassar)
  handle_path /api/* {
    reverse_proxy api:3000
  }

  # Frontend (Vite dev, com WS/HMR)
  reverse_proxy web:5173
}
